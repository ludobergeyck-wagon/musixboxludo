<div class="min-vh-100 px-3" style="padding-top: 80px; padding-bottom: 30px;">
  <div class="container" style="max-width: 500px;">

    <!-- Header -->
    <div class="text-center mb-4">
      <h1 class="fw-bold">🎵 My Playlists</h1>
    </div>

    <!-- Bouton créer -->
    <div class="d-grid mb-4">
      <%= link_to new_playlist_path, class: "btn btn-warning border-dark border-2 fw-bold py-3" do %>
        ➕ Create new playlist
      <% end %>
    </div>

    <!-- Liste des playlists -->
    <% if @playlists.present? %>
      <% @playlists.each do |playlist| %>
        <div class="card border-dark border-2 mb-3 shadow-sm">
          <div class="card-body d-flex justify-content-between align-items-center">
            <div>
              <h5 class="fw-bold mb-1"><%= playlist.title %></h5>
              <small class="text-muted"><%= playlist.songs.count %> songs</small>
            </div>
            <div class="d-flex gap-2">
              <%= link_to playlist_path(playlist), class: "btn btn-outline-dark btn-sm" do %>
                👁️
              <% end %>
              <%= button_to playlist_path(playlist), method: :delete, class: "btn btn-outline-danger btn-sm", data: { turbo_confirm: "Delete this playlist?" } do %>
                🗑️
              <% end %>
            </div>
          </div>
        </div>
      <% end %>
    <% else %>
      <div class="text-center text-muted py-5">
        <p class="fs-1">🎧</p>
        <p>No playlists yet. Create your first one!</p>
      </div>
    <% end %>

    <!-- Back link -->
    <div class="text-center mt-4">
      <%= link_to "← Back to Profile", profile_path, class: "btn btn-outline-dark fw-bold" %>
    </div>

  </div>
</div>
