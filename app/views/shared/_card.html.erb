<div class="card-result text-center" data-controller="card" data-card-target="cardResult">
  <!-- Image de fond -->
  <img class="card-image" src="<%= asset_path('Logo_sans_nom_2.png') %>" alt="photos" data-card-target="backgroundImage">

  <!-- Photo utilisateur -->
  <img id="userPhoto" data-card-target="userPhoto" alt="Your photo">

  <!-- Overlay avec stats -->
  <div class="card-overlay">

    <% if @percentage > 80 %>
      <div class="trophy-card">
        <h3 class="trophy-title">🏆 Ace of <%= @playlist.title %> 🏆</h3>
        <% if @percentage == 100 %>
          <p class="trophy-perfect">Perfect score!</p>
        <% end %>
      </div>
    <% end %>

    <% if @fastest_title %>
      <div class="fastest-card">
        <div class="fastest-label">🎯 Fastest guess</div>
        <div class="fastest-time"><%= (@fastest_title.time_taken / 1000.0).round(2) %> sec</div>
        <% if @fastest_title.song %>
          <div class="fastest-song"><%= @fastest_title.song.title %></div>
        <% end %>
      </div>
    <% end %>

    <div class="card-footer">
      <span>🎸 MusixBoxx</span>
      <span>musixboxx.com</span>
    </div>

  </div>
</div>
